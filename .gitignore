# -----------------------------------------------------------------------------
# Governance
# Repository ignore rules
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Local scratch files
# -----------------------------------------------------------------------------

# commit_message.txt: reusable local file for composing multi-line commit
# messages (git commit -F commit_message.txt). Cleared and rewritten before
# each use, never committed.
commit_message.txt